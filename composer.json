{
    "require": {
        "doctrine/orm": "*",
        "symfony/cache": "^7.0",
        "ramsey/uuid-doctrine": "^2.0",
        "symfony/flex": "^2.4"
    },
    "config": {
        "allow-plugins": {
            "symfony/flex": true
        }
    }
}
